# No boto3 here on purpose -- it ships preinstalled in every AWS Lambda
# Python managed runtime (including 3.13), and this function only needs
# whatever version that is (a plain get_object call). Real incident: `sam
# build` failed with "Could not satisfy the requirement: boto3>=1.34" --
# pip inside the build container couldn't resolve it from PyPI. Since
# app.py already imports boto3 lazily (see _get_s3_client()), dropping the
# pin here removes that failure mode entirely rather than just retrying
# past a flaky network moment.
pillow>=10.0
